{
  "name": "brokex-bet",
  "version": "1.0.0",
  "description": "Crypto bet market, built with Chainlink Oracle",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:testnet": "npx hardhat run --network testnet scripts/deploy.ts",
    "deploy:mainnet": "npx hardhat run --network mainnet scripts/deploy.ts",
    "test": "npx hardhat test",
    "test:coverage": "npx hardhat coverage",
    "test:size-contracts": "npx hardhat size-contracts"
  },
  "devDependencies": {
    "@chainlink/contracts": "^0.2.0",
    "@nomiclabs/hardhat-web3": "^2.0.0",
    "@openzeppelin/contracts": "^4.2.0",
    "@openzeppelin/test-helpers": "^0.5.15",
    "test-helpers": "^0.1.4",
    "web3": "^1.7.5"
  },
  "dependencies": {
    "hardhat": "^2.10.2"
  }
}
